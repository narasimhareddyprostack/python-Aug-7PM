# No of elements in each row = i+1
# With a row has no element are changing
# If elements are keep on changing, we have use nested or innter loop.

n=int(input("Please Enter Number: "))
for i in range(n):
    print('* ' * (i+1))

